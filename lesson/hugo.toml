baseURL = "/"
languageCode = "en-us"
title = "Building a VLA by Hand"
enableGitInfo = false

# Every page ships as a relative link so the site works from a file:// path,
# a subdirectory, or a static host without rebuilding.
relativeURLs = true
uglyURLs = false

[markup.goldmark.renderer]
unsafe = true

[markup.highlight]
noClasses = false
style = "github"
lineNos = false
tabWidth = 2

[markup.tableOfContents]
startLevel = 2
endLevel = 3

[params]
description = "Turning a 460M-parameter vision-language model into a robot policy, one measurement at a time."
repo = "https://github.com/bobcowher/franka-kitchen-vla1"
